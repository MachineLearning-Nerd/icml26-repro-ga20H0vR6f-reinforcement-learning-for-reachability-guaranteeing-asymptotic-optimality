# Claim 3 evaluator entry

Fixed command: `uv run --frozen python -m repro.run`

Primary verifier: `repro/claim3.py`. Independent checker: `repro/check_claim3.py`. The command prints all proof obligations and metadata inline and exits nonzero if any obligation fails.
