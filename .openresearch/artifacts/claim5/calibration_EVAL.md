# Claim 5 calibration evaluator note

Fixed command: `uv run --frozen python -m repro.run`

The calibration is a release blocker check, not a claim verdict. It must show nine hash-verified official inputs, a nontrivial converted `ij.3` model, at least two learning iterations, and rejection of a tampered-input control before the full ten-trial benchmark node is created.
