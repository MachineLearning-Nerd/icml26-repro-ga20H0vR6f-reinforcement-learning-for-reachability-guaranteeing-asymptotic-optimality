# Claim 5 evaluator entry

Run the fixed repository command. It must complete all 90 trials, print every raw trial and benchmark curve in `BEGIN_REPRO_EVIDENCE_JSON`, pass the independent completeness/aggregation checker, and reject the missing-trial control. The final scientific verdict depends on the observed comparison, not merely completion.
