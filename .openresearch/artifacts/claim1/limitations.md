# Claim 1 limitations and deviations

The executable certificate verifies the existential theorem, not the practical usefulness of its potentially enormous witness. The PAC-BVI implication uses the conservative two-sided transition-confidence variant identified by the cited 2019 primary source. The current authors' heuristic `N_k` is deliberately not treated as theorem evidence. Standardized-benchmark behavior is assessed separately under Claim 5.
